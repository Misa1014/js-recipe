const genkiFunktion = function(number) {
  for (let n = 1; n <= number; n++) {
    if (n % 3 === 0) {
      console.log(`${n} !!!!!`)
    } else if{
        const strNum = String(n)
    }
      console.log(n)
    }
  }
}
